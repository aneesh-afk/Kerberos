# Kerberos TGS (Group 3)

Port: 8002

python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload --port 8002

See docs/team/group-3-kerberos-core.md and docs/crypto-interface.md.
